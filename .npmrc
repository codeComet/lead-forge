# Don't run an implicit `pnpm install` check before every `pnpm run` script.
# Avoids a pre-run failure and keeps script startup fast.
verify-deps-before-run=false

# Hoist enough for Next.js + tooling to resolve cleanly in the monorepo.
shamefully-hoist=true
